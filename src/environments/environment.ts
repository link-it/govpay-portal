/**
 * Configurazione ambiente development
 *
 * Questo file viene usato di default durante lo sviluppo.
 * Per la build di produzione viene sostituito con environment.prod.ts
 */
export const environment = {
  production: false,
};
