/**
 * Configurazione ambiente production
 *
 * Questo file sostituisce environment.ts durante la build di produzione.
 * Configurato in angular.json sotto fileReplacements.
 */
export const environment = {
  production: true,
};
